package repository

import "testing"

func BenchmarkBootstrap(b *testing.B) {

	//conn :=
	//db, err := NewDBStorage()
	//b.Run("recursive", func(b *testing.B) {
	//	for i := 0; i < b.N; i++ {
	//		FiboRecursive(count)
	//	}
	//})
	//b.Run("optimized", func(b *testing.B) {
	//	for i := 0; i < b.N; i++ {
	//		FiboOptimized(count)
	//	}
	//})
}
